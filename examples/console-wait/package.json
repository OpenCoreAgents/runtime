{
  "name": "@agent-runtime/example-console-wait",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Interactive console: model emits wait, user types a line, RunBuilder.onWait resumes in-process.",
  "scripts": {
    "start": "tsx src/main.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@agent-runtime/core": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.3",
    "typescript": "^5.7.3"
  }
}
