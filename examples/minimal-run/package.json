{
  "name": "@agent-runtime/example-minimal-run",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Minimal runnable demo: mock LLM + in-memory memory, one agent.run() — no API keys.",
  "scripts": {
    "start": "tsx src/main.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@agent-runtime/core": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.3",
    "typescript": "^5.7.3"
  }
}
