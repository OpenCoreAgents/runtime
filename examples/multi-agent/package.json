{
  "name": "@agent-runtime/example-multi-agent",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "In-process multi-agent demo: InProcessMessageBus, send_message (event + request/reply), mock LLM — no API keys.",
  "scripts": {
    "start": "tsx src/main.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@agent-runtime/core": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.3",
    "typescript": "^5.7.3"
  }
}
